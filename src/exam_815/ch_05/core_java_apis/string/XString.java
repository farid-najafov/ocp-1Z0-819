package exam_815.ch_05.core_java_apis.string;

public class XString {
//    String s = true + 1;  // won't compile. One of operands must be string.

//    Object obj = "str";
//    String s = obj + obj;
    // won't compile. Even though obj points to string at runtime, for compiler it is an Object, not a String

    public static void main(String[] args) {

    }
}
